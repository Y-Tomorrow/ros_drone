/**
 * @file utils.h
 * @brief 兼容旧代码的工具函数集合。
 *
 * 说明：
 * - 工程历史代码里 DataManager.cpp 依赖 "utils.h"。
 * - 目前项目已有较新的 <ros_drone/util.h>（header-only）。
 * - 这里提供最小兼容层，避免大范围重构。
 */
#pragma once

#include <ros_drone/util.h>

